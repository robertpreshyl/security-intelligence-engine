# AI-SOC Integration Project — All Dependencies
# Python 3.12+
# Install: pip install -r requirements.txt

# ── Core ──
requests>=2.31.0
urllib3>=2.0.0
python-dotenv>=1.0.0
tabulate>=0.9.0

# ── API Server ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# ── Dashboard ──
streamlit>=1.28.0
pandas>=2.1.0

# ── Report Export (Optional) ──
markdown>=3.5.0

# For PDF export, install ONE of these (both require system dependencies):
# weasyprint>=60.0  # Recommended - requires: libpango, libcairo (apt install libpango-1.0-0 libcairo2)
# pdfkit>=1.0.0     # Alternative - requires: wkhtmltopdf (apt install wkhtmltopdf)
